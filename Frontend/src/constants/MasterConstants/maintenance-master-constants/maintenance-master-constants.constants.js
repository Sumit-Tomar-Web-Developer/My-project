import { API_BASE_URL } from '../../constants';

export const MAINTENANCE_LIST = `${API_BASE_URL}/fetchMaintenance`;
export const MAINTENANCE_UPDATE_LIST = `${API_BASE_URL}/saveMaintenance`;
export const MAINTENANCE_DELETE_LIST = `${API_BASE_URL}/deleteMaintenance`;
