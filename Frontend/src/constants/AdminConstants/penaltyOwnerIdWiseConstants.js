import { API_BASE_URL } from '../constants';

export const GET_SEARCHED_PROPERTIES = `${API_BASE_URL}/searched-properties`;

export const APPLY_PENALTY_BY_OWNER_ID = `${API_BASE_URL}/penalty-owner-id-wise`;
