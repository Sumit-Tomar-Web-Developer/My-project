import { API_BASE_URL } from 'constants/constants';

export const GET_ACCESS_LEVELS = `${API_BASE_URL}/access-levels`;
export const SAVE_PAGE_PERMISSIONS = `${API_BASE_URL}/save-pages-permissions`;
export const FETCH_SAVED_PERMISSIONS = `${API_BASE_URL}/saved-permissions`;

export const GET_PAGE_ID = `${API_BASE_URL}/pageId-by-pageName`;
